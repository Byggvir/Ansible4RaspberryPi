---

# Install additional  nice to have packges

- name: apt install initial packages
  become: yes
  apt:
    name:
        - aptitude
        - findutils
        - fortune
        - pwgen
        - sudo
        - vim
    update_cache: yes

...
